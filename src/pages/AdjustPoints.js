import React from 'react'
import AdjustPointButton from '../components/AdjustPointButton'

const AdjustPoints = () => {
  return (
    <>
        <AdjustPointButton />

      {/* <div>
        {userRole === "Admin" && (
          <AdjustPointForm loggedUser={loggedUser} />
          )}
        {userRole === "Distributor" && (
          <AdjustPointForm loggedUser={loggedUser} />
          )}
        {userRole === "Stokist" && (
          <AdjustPointForm loggedUser={loggedUser} />
          )}
        {userRole === "Player" && (
          <AdjustPointForm loggedUser={loggedUser} />
        )}
      </div> */}
    </>
  )
}

export default AdjustPoints